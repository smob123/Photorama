query getUserNotifications($userId: String!, $jwt: String!, $startIndex: Int!, $endIndex: Int!) {
  getUserNotifications(userId: $userId, jwt: $jwt, startIndex: $startIndex, endIndex: $endIndex) {
    postId
    followerName
    message
    userAvatar
    postImage
    datetime
  }
}