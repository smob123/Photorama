package com.example.photorama.heplerObjects

/**
 * @author Sultan
 * a custom object that's used to store a user's info that can be acquired from different
 * server requests.
 */

class UserListItemType(
    var userId: String,
    var username: String,
    var screenName: String,
    var avatar: String
)
